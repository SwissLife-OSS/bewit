<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Version.props', '$(MSBuildThisFileDirectory)..\src\'))"/>

  <PropertyGroup>
    <TargetFramework>net6.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
